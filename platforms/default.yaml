platform: generic
# ⚠️ 触发词同步：此文件中的 trigger_words 需与 SKILL.md（权威来源）保持同步。
# 修改触发词时：SKILL.md → 同步所有 platforms/*.yaml
scan_paths:
  user: "~/.agent-skills/*/"
  plugin: ""
archive_dir: "~/.agent-skills-archived/"
trigger_words: ["skills-audit", "project-audit", "技能审查", "技能审计"]
project_scan_default_depth: 8
